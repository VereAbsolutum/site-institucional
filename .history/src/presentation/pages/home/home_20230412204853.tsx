import React from 'react';
import { Header } from '../../components';

const home: React.FC = () => {
    return (
        <main>
            <Header />
            <div>

            </div>
        </main>

    );
};

export default home;